/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import AuthPrv from './components/AuthPrv';

AppRegistry.registerComponent(appName, () => AuthPrv);
